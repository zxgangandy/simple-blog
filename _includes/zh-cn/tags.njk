<div class="tag-list">
  {% set allowTags = ['前端', '后端', '算法'] %}
  {% for tag in collections.tagList %}
    {% if tag in allowTags %}
      {% set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
      <a class="tag-list__item {% if tag === currentTag %}tag-list__item--active{% endif %}" href="{{ tagUrl | url }}">{{ tag }}</a>
    {% endif %}
  {% endfor %}
</div>
